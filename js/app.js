function messageNA() {
    alert("Sorry, this source is not available yet.  To be uploaded soon.");
}

function hover_start(img_id) {
    document.getElementById(img_id).style.opacity = "1";
}
  
function hover_stop(img_id) {
    document.getElementById(img_id).style.opacity = "0";
}